# Porta do servidor (padrão: 3000)
PORT=3000

# Ambiente: development | production
NODE_ENV=development

# CORS: origens permitidas, separadas por vírgula.
# Em produção use lista explícita (ex: https://app.empresa.com).
# Deixe vazio ou não defina para aceitar qualquer origem (apenas desenvolvimento).
# CORS_ORIGINS=https://app.empresa.com,https://admin.empresa.com

# Prefixo global da API (padrão: api)
# GLOBAL_PREFIX=api

# Logo da loja no PDF do resumo de vendas (caminho relativo ao projeto ou absoluto). Opcional.
# STORE_LOGO_PATH=assets/logo.png

# Persistência: in_memory | postgres
# PERSISTENCE_DRIVER=postgres

# Banco (PostgreSQL). Em produção use migrations; não use DB_SYNCHRONIZE=true.
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=suprema_pdv
# DB_SYNCHRONIZE=true
# DB_LOGGING=false
# DB_MIGRATIONS=true
